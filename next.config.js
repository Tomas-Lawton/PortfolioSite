/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
    images: {
      domains: ['raw.githubusercontent.com', 'miro.medium.com', 'i.ibb.co', 'lh4.googleusercontent.com'], 
    },
}

module.exports = nextConfig
